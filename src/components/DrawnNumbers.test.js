import React from 'react'
import { render, fireEvent } from "react-testing-library";
import "jest-dom/extend-expect";
import DrawnNumbers from './DrawnNumbers'

test("first text", () => {
    // const {getByText} = render(<DrawnNumbers/>)



})